# Repo-scoped Codex command approval rules.

prefix_rule(
    pattern = ["npm", "run", "build"],
    decision = "allow",
    justification = "Allow the Angular playground build script for this repository outside the sandbox.",
    match = [
        "npm run build",
    ],
    not_match = [
        "npm run",
        "npm run build:ngx-loading-buttons",
        "npm run watch",
        "npm install",
    ],
)
